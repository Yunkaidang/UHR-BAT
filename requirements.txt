# Recommended setup for conda env `geo`:
#   conda activate geo
#   pip install torch==2.1.2+cu121 torchvision==0.16.2+cu121 torchaudio==2.1.2+cu121 --index-url https://download.pytorch.org/whl/cu121
#   pip install flash-attn==2.7.3 --no-build-isolation --no-cache-dir
#   pip install -r requirements.txt --no-deps
#   cd with_k-means/longva && pip install -e . --no-deps
# or:
#   cd with-SAM/longva && pip install -e . --no-deps
#
# This file is based on with-SAM/requirements.txt and supplemented with
# additional dependencies referenced by longva/pyproject.toml and the train/eval code.

# Core dependencies shared by with-SAM and with_k-means
accelerate==0.34.2
datasets==2.16.1
deepspeed==0.14.5
numpy==1.26.4
pandas==2.2.3
pillow==11.0.0
tensorboard==2.19.0
tokenizers==0.19.1
tqdm==4.67.1
transformers==4.43.4
triton==2.1.0

# LongVA training / evaluation dependencies
einops==0.6.1
einops-exts==0.0.4
ftfy
huggingface_hub
httpx==0.24.0
open_clip_torch
packaging
peft==0.4.0
pydantic==1.10.8
PyYAML
requests
safetensors
scikit-learn==1.2.2
scipy
sentencepiece==0.1.99
shortuuid
timm
tyro

# Common media / logging utilities used by training or video code paths
av
decord
opencv-python
wandb==0.16.5

# CUDA / platform-specific packages: install separately to match your machine
# flash-attn==2.7.3
# bitsandbytes==0.41.0
# torch==2.1.2+cu121
# torchaudio==2.1.2+cu121
# torchvision==0.16.2+cu121
